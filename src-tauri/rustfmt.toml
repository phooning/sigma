# src-tauri/rustfmt.toml
trailing_comma = "Always" # or "Never" if you prefer the opposite

# Optional but recommended for consistency
max_width = 100
hard_tabs = false
newline_style = "Unix"
use_small_heuristics = "Max" # helps with method chains
