{
  "extends": "./tsconfig.json", // or "./tsconfig.app.json"
  "compilerOptions": {
    "composite": true,
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "types": ["node", "jsdom", "vitest/globals"], // add vitest types if using globals
  },
  "include": ["src/**/*.test.ts", "src/**/*.test.tsx", "vitest.config.ts"],
}
